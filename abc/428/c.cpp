#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
int main() {
    int q;
    cin >> q;
    while(q--){
        int x;
        cin >> x;
        if(x==1){
            char c;
            cin >> c;
        }
        else{
            
        }
    }
}