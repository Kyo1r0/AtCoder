#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
int main() {
    int n,q;
    string s;
    cin >> n >> q >> s;
    vector<int> x(n);
    vector<char> c(n);
    rep(i,n){
        cin >> x[i] >> c[i];
    }

    

}