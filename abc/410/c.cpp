#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
int main() {
    int n,q;
    cin >> n >> q;
    vector<int> a(n+1);
    for(int i=0;i<n+1;i++)a[i] = i;
    for(int i=0;i<q;i++){
        int check;
        cin >> check;
        if(check==1){

        }
        else if(check==2){

        }
        else{

        }
    }
}